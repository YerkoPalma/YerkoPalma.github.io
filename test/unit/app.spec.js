/* global describe, it, expect, beforeAll, afterAll */
describe('app.js', function () {
  // pending
  it('should include all needed components')

  // pending, production
  it('should include a single script tag')

  // pending, production
  it('should have all the minimu SEO config on the head')
})

describe('data', function () {
  // test all the data and metadata from config.js and media folder
  it('should include all the global data correctly')
})
